# 工作区环境说明

这个文件夹下的内容是：我跑仿真的数据 + 我现在使用的 bridge + 分析数据的脚本。

仿真的环境是：Carla 0.9.15 部署在服务器端，Apollo 10.0.0 系统部署在 Orin 侧，bridge 在服务器侧（Carla 与 Orin 通过网线连接）。

当前 Apollo 未发送 Guardian 模块命令给 bridge，bridge 直接读取 control 给出的命令。

## 实验数据分析口径

### 响应阶段距离

`D_delay` 统一采用车辆速度对墙钟时间的梯形积分：

\[
D_{\mathrm{delay}}=\int_{t_1}^{t_2}v(t)\,\mathrm{d}t_{\mathrm{wall}}.
\]

CARLA 在不同run中的推进帧数和实时因子可能不同，不能将CARLA仿真帧数、仿真时间或Localization空间位移与墙钟速度积分结果混用。若需要保留这些诊断量，必须使用独立且含义明确的字段，例如 `D_delay_wall_integral_m`、`D_delay_sim_observed_m`、`e2e_sim_frames` 和 `realtime_factor`；报告中的主 `D_delay` 使用墙钟速度积分结果。

不同run之间比较 `D_delay` 时，所有run必须采用相同的墙钟时间基准、积分方法和端点定义，禁止只修正部分run后与其他口径的结果放在同一列比较。

### 实际数据结果与模型结果

分析必须同时计算并分别保存以下两套结果，字段名和表头应明确标注 `data/observed` 与 `model/predicted`，禁止用其中一套覆盖另一套：

1. **实际数据计算结果**：使用该run实际测得的速度、墙钟时间、距离、制动过程、最终净距和碰撞结果，按照统一公式计算。实际输入缺失时应标记为不可用，并说明缺失原因，不能使用模型预测值补充成“实际结果”。
2. **模型计算结果**：使用经验模型或其他预测模型得到制动距离、碰撞余量和安全余量，用于评价模型预测能力、误差和适用范围。

在编写实验结果分析报告时，主结果、结论和逐run结果表使用实际数据计算结果；模型结果应单独列入模型分析或预测对照部分。

在测试模型是否准确时，将模型计算结果与端点定义、时间基准和距离口径一致的实际数据结果进行比较，并报告绝对误差、相对误差及误差方向。此时才使用模型结果评价预测准确性。

## 代码修改原则

修改代码前必须先判断该修改是否必要，并确认问题根因能够由该修改直接解决。

优先采用最小修改方案，尽量保持原有接口、流程、行为和兼容性，不进行与当前问题无关的重构或扩展。

修改前必须评估对原有代码的影响、潜在副作用和性能压力。只有最小修改无法解决问题时，才允许扩大修改范围。

目标是用最少的代码高效、准确地解决问题；代码改动数量不是衡量工作质量的标准。
