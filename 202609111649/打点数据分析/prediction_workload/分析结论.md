# Prediction逐帧障碍物与工作量\ntrace帧1193，输入/输出均匹配的完整帧1102，执行尾部候选11。\n按record序号及trace身份关联，保留零障碍物消息。缺失输出留空。Caution/Normal/Ignore互斥；Interactive是独立标签。\n局部对照使用前后20帧、相同障碍物数量且非P99尾部的完整帧；只是描述基线，不是因果对照。\n工作量字段与不可恢复的group信息见新增工作量字段说明.md。