# 六层逻辑关系：从时间扰动到物理安全退化

这六层可以概括成一条非常清晰的研究逻辑：

$$
\boxed{
\text{故障源}
\rightarrow
\text{时间症状}
\rightarrow
\text{系统级时间表征}
\rightarrow
\text{实时性判定}
\rightarrow
\text{物理传播}
\rightarrow
\text{安全后果}
}
$$

这六层分别回答六个不同的问题。

需要特别说明一点：**Dynamic Deadline 应由车辆物理状态独立产生，再和实际 Reaction Time 比较；它不应被理解为 Cause-Effect Chain Timing 的下游结果。** 实时系统中的 cause-effect chain 用 reaction time 和 data age 描述实际时间行为，而 deadline 是用于约束这些时间行为的要求。

---

# 一、第一层：Temporal Disturbance —— 时间扰动

回答：

> **系统受到了什么时间上的扰动？**

这是整个研究的“输入变量/故障源”。

例如：

$$
F_T\in\{Delay,\ Jitter,\ Backlog,\ Gap,\ Staleness,\ Phase,\ Interference\}
$$

包括：

- 固定时延；
- 随机抖动；
- 任务执行时间异常；
- CPU/GPU资源竞争；
- 消息排队；
- 网络时延；
- 更新丢失；
- 周期/相位错位；
- 数据延迟。

这一层只描述：

> **发生了什么时间扰动。**

暂时不评价它是否危险。

例如：

> Control链路持续增加300 ms。

这只是一个 **Temporal Disturbance**。

---

# 二、第二层：Temporal Behavior Degradation —— 时间行为退化

回答：

> **这个时间扰动在系统运行中表现成了什么异常时间现象？**

这里至少有三个并列维度。

## 1. Response-time Variability

$$
T_R^{(1)},T_R^{(2)},...,T_R^{(n)}
$$

不同run、不同帧的响应时间出现：

- 增长；
- 抖动；
- 长尾；
- 异常峰值。

## 2. Data Freshness Degradation

例如：

$$
Age=t_{\mathrm{current}}-t_{\mathrm{source}}
$$

系统仍然在输出，但数据代表的是几百毫秒以前的环境。

## 3. Update Continuity Degradation

定义：

$$
G_k=t_{k+1}^{out}-t_k^{out}
$$

例如：

```text
100 ms → 100 ms → 100 ms → 500 ms → 100 ms
```

这种异常更新空窗。

因此这一层的作用是：

> **把抽象的时间扰动变成系统内部能够观测到的时间症状。**

这三个指标需要并列处理。Cause-effect chain研究中，reaction time 与 data age本来就是不同的端到端时间语义。

---

# 三、第三层：Cause-Effect Chain Timing —— 因果链时间行为

这是非常关键的一层。

第二层关注：

> 哪里出现时间异常。

第三层开始回答：

> **这些局部时间异常最终怎样影响从环境输入到车辆动作的完整因果链？**

例如：

$$
Sensor
\rightarrow
Perception
\rightarrow
Prediction
\rightarrow
Planning
\rightarrow
Control
\rightarrow
Actuation
$$

实时系统中的cause-effect chain本身就是为了研究数据通过一系列相关任务传播时的**端到端时间属性**。

这里最重要的是两个量。

## Reaction Time

$$
T_R=t_{\mathrm{physical\ response}}-t_{\mathrm{cause}}
$$

回答：

> 一个外部事件出现以后，多久真正影响车辆？

例如：

$$
t_1\rightarrow t_2
$$

## Data Age

$$
A=t_{\mathrm{effect}}-t_{\mathrm{source}}
$$

回答：

> 车辆做出这个动作的时候，它依据的环境信息有多旧？

所以第二层和第三层的关系是：

$$
\boxed{\text{局部时间异常}\rightarrow\text{端到端时间效应}}
$$

比如：

```text
Detection queue增加
          │
Fusion data age增加
          │
Planning更新错过周期
          │
Control晚发布
          ↓
最终Reaction Time增加
```

---

# 四、第四层：Real-Time Constraint —— 实时约束判定

这一层解决整个课题最核心的问题：

> **慢到什么程度才算真正的实时性问题？**

这里必须引入：

$$
\tau^*
$$

也就是：

> **Dynamic Physical Deadline / 车速隐形Deadline**

## 更严谨的结构应当是两条线汇合

```text
实际系统时间行为                      当前物理状态
Cause-Effect Chain                   Vehicle State
      │                                  │
      ↓                                  ↓
Reaction Time TR              Dynamic Deadline τ*
      │                                  │
      └──────────────┬───────────────────┘
                     ▼
             Timing Slack
              S = τ* - TR
```

原因在于：

### $T_R$

是系统**实际用了多久**。

### $\tau^*$

是物理世界**最多允许多久**。

自动驾驶面对的是动态变化的deadline，而非所有情况下固定一个截止时间。

本研究可以进一步把deadline建立在：

$$
\tau^*=f(v,D,a,\mu,\text{safety margin},...)
$$

上。

也就是说：

- 车速越高；
- 障碍物越近；
- 制动能力越弱；

系统还能允许的响应时间越短。

---

# 五、为什么 Timing Slack 是整个六层结构的“判决点”

定义：

$$
\boxed{S_T=\tau^*-T_R}
$$

它完成了从：

> 性能问题

到：

> 实时性问题

的转换。

### 情况A

$$
T_R=800ms
$$

但：

$$
\tau^*=1200ms
$$

则：

$$
S_T=400ms
$$

系统虽然比较慢，但时间约束仍满足。

### 情况B

$$
T_R=300ms
$$

但：

$$
\tau^*=250ms
$$

则：

$$
S_T=-50ms
$$

已经违反实时约束。

所以真正的实时性判定是：

$$
\boxed{T_R>\tau^*}
$$

即：

$$
\boxed{Deadline\ Miss}
$$

因此前四层其实是在完成：

$$
\boxed{
\text{Temporal disturbance}
\rightarrow
\text{Temporal behavior}
\rightarrow
\text{Reaction time}
\rightarrow
\text{Deadline violation?}
}
$$

到这里都还是“时间域”。

---

# 六、第五层：Temporal-to-Physical Propagation —— 时间向物理空间传播

这一层是本研究最有特色的位置。

回答：

> **Deadline miss发生以后，几百毫秒到底意味着什么？**

自动驾驶车辆在运动。

因此：

$$
\Delta t
$$

能够通过速度积分直接转化成：

$$
\Delta d
$$

## 先定义 Response Distance

从环境事件出现到车辆真正响应：

$$
D_R=\int_{t_c}^{t_e}v(t)\,dt
$$

这是整个响应过程中车辆走掉的距离。

## 再定义 Distance Debt

假设：

$$
t_d=t_c+\tau^*
$$

是最晚允许响应时刻。

实际响应：

$$
t_e>t_d
$$

那么：

$$
\boxed{D_{\mathrm{debt}}=\int_{t_d}^{t_e}v(t)\,dt}
$$

也可以写成：

$$
D_{\mathrm{debt}}
=\max\left(0,\int_{t_d}^{t_e}v(t)dt\right)
$$

它的物理解释非常直接：

> **系统的时间预算已经耗尽以后，车辆因为响应仍未形成而继续前进的距离。**

所以：

$$
\boxed{Deadline\ Miss\xrightarrow{\int vdt}Distance\ Debt}
$$

这一条就是“计算机实时系统”和“汽车动力学”之间的桥。

---

# 七、第六层：Physical Safety —— 物理安全结果

有了Distance Debt以后，就能计算：

> **这笔时间债务消耗了多少真实制动空间？**

原本可用空间：

$$
D_{\mathrm{available}}
$$

由于时间失守，损失：

$$
D_{\mathrm{debt}}
$$

所以实际剩余制动空间变成：

$$
D_{\mathrm{brake,available}}
=
D_{\mathrm{available}}
-
D_{\mathrm{response}}
$$

再和车辆真正需要的制动距离比较：

$$
M_D
=
D_{\mathrm{brake,available}}
-
D_{\mathrm{brake,required}}
$$

得到：

> **Physical Safety Margin**

### 如果

$$
M_D>0
$$

还能够停车。

可能只是：

> near-critical stop。

### 如果

$$
M_D\rightarrow0
$$

进入：

> safety-margin depletion。

### 如果

$$
M_D<0
$$

就可能产生：

- minimum-distance violation；
- emergency maneuver；
- near miss；
- collision。

因此：

$$
\boxed{Deadline\ Miss\not\Rightarrow Collision}
$$

Deadline miss首先造成：

$$
Distance\ Debt
$$

然后：

$$
Distance\ Debt\rightarrow Braking\ Margin\ Loss
$$

在具体车辆状态下，最终才可能导致Collision。

---

# 八、六层真正的逻辑关系

可以整理成下面这张表：

| 层 | 名称 | 核心问题 | 性质 |
|---|---|---|---|
| L1 | Temporal Disturbance | 发生了什么时间扰动？ | **原因** |
| L2 | Temporal Behavior Degradation | 系统暴露出了什么时间异常？ | **症状** |
| L3 | Cause-Effect Chain Timing | 异常如何传播成端到端时间行为？ | **系统级表征** |
| L4 | Dynamic Deadline & Timing Slack | 这种时间行为是否违反当前物理时间约束？ | **实时性判据** |
| L5 | Distance Debt | 时间失效造成多少额外空间损失？ | **传播机制** |
| L6 | Physical Safety Degradation | 空间损失最终导致什么安全后果？ | **结果** |

所以最简洁的关系其实就是：

$$
\boxed{
Cause
\rightarrow
Manifestation
\rightarrow
System\ Effect
\rightarrow
Violation
\rightarrow
Propagation
\rightarrow
Consequence
}
$$

即：

$$
\boxed{
原因
\rightarrow
症状
\rightarrow
系统效应
\rightarrow
约束失守
\rightarrow
物理传播
\rightarrow
安全后果
}
$$

---

# 九、更深一层：它实际上是闭环，不是纯单向链

这一点以后做实车尤其重要。

车辆状态：

$$
x(t)=[v,D,a,\ldots]
$$

决定：

$$
\tau^*(x(t))
$$

但时间故障又会改变车辆状态。

例如：

$$
Temporal\ Disturbance
$$

导致：

$$
Reaction\ Time\uparrow
$$

导致：

$$
D_{\mathrm{response}}\uparrow
$$

导致：

$$
D_{\mathrm{clear}}\downarrow
$$

于是新的：

$$
\tau^*(x)\downarrow
$$

也就是说：

> **系统越晚响应，剩余距离越小；剩余距离越小，之后允许的deadline又越紧。**

形成：

$$
\boxed{
Timing\ degradation
\rightarrow
State\ degradation
\rightarrow
Tighter\ deadline
}
$$

这就是典型的Cyber-Physical闭环效应。

所以未来论文完整版最好画一个反馈箭头：

```text
Distance Debt
     │
     ▼
Vehicle State Changes
     │
     ▼
Dynamic Deadline τ*
```

这比纯粹从上往下的一条直线更准确。

---

# 十、最终建议固定的六层命名

以后报告、论文、实验设计都可以统一：

### Layer 1 — Temporal Disturbance Layer
**时间扰动层**

↓

### Layer 2 — Temporal Degradation Layer
**时间行为退化层**

↓

### Layer 3 — Cause-Effect Timing Layer
**因果链时间表征层**

↓

### Layer 4 — Temporal Correctness Layer
**时间正确性判定层**

↓

### Layer 5 — Temporal-to-Physical Propagation Layer
**时间—物理传播层**

↓

### Layer 6 — Physical Safety Layer
**物理安全结果层**

把它压成一句话：

> **前3层负责发现和量化自动驾驶系统的时间异常，第4层判断这种异常是否已经构成时间正确性失效，第5层解释时间失效如何转化为空间损失，第6层验证这种空间损失是否最终造成车辆安全后果。**

而整个研究最核心的两座桥是：

$$
\boxed{
Reaction\ Time
\xrightarrow{\text{compare}}
Dynamic\ Deadline
}
$$

完成**性能问题 → 实时性问题**的转换；

以及：

$$
\boxed{
Deadline\ Miss
\xrightarrow{\int vdt}
Distance\ Debt
}
$$

完成**计算机时间问题 → 车辆物理安全问题**的转换。

这两个“转换”如果做扎实，整个课题的理论主线就会非常清楚。

---

# 参考资料

1. ROS 2 Design: *Introduction to Real-time Systems*  
   https://design.ros2.org/articles/realtime_background.html

2. Günzel, M., Teper, H., Chen, K.-H., von der Brüggen, G., & Chen, J.-J. (2023). *On the Equivalence of Maximum Reaction Time and Maximum Data Age for Cause-Effect Chains*. ECRTS 2023.  
   https://drops.dagstuhl.de/entities/document/10.4230/LIPIcs.ECRTS.2023.10

3. D3 / Dynamic Deadline-Driven autonomous-driving related work  
   https://dl.acm.org/doi/10.1145/3798279

4. Shalev-Shwartz, S., Shammah, S., & Shashua, A. *On a Formal Model of Safe and Scalable Self-driving Cars* (RSS).  
   https://arxiv.org/pdf/1708.06374

5. Koopman, P., Osyk, B., & Weast, J. *Autonomous Vehicles Meet the Physical World: RSS, Variability, Uncertainty, and Proving Safety*.  
   https://arxiv.org/abs/1911.01207
